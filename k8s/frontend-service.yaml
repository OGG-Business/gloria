apiVersion: v1
kind: Service
metadata:
  name: swiftpay-frontend
  namespace: swiftpay
  labels:
    app: swiftpay-frontend
    component: frontend
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: swiftpay-frontend