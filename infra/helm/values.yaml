image:
  repository: ghcr.io/your-org/openbank-backend
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

env:
  APP_APP_ENV: prod
  APP_AUTH_DISABLED: "false"
  APP_DATABASE_URL: postgresql://user:pass@cloudsql:5432/transfers